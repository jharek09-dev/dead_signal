# Copy to .env and fill in. .env is gitignored — never commit real keys.

# Your Anthropic API key (used only by the Vite dev proxy, never sent to the browser).
ANTHROPIC_API_KEY=sk-ant-...

# Point the game at the dev proxy so the key is injected server-side.
VITE_API_URL=/api/messages
